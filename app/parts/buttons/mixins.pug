mixins 

    mixin pet(name)
     li.pet= name

    mixin list
     ul
         li foo
         li bar
         li baz

     mixin list(id, ...items)
        ul(id=id)
             each item in items
                 li= item

+list('my-list', 1, 2, 3, 4)

// class="butt n1" 
  mixin button(text, ...items)
     input(class!=attributes.class type="button" class=...items value=text)    